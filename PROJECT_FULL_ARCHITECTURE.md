# AI Game Security Lab 全量架构总览

本文档面向协作开发人员与其他语言模型，用自然语言完整说明项目的模块职责、数据流位置、入口脚本、测试工具和资源目录。本文档不包含任何代码片段、函数签名、类定义或代码块，仅描述系统如何组织、如何流转、以及每个部分解决什么问题。[cite: 1]

## 1. 项目定位与核心数据流

本项目是一个围绕游戏视觉理解、目标筛选、轨迹规划、动作模拟、行为分析与反作弊检测展开的研究型平台，主要服务于离线、可控、合法的实验环境。[cite: 1] 它的核心价值不在于单点算法，而在于把从屏幕画面到行为判别的整条链路串起来，支持视觉推理、控制模拟、统计建模和对抗研究的连续实验。[cite: 1]

项目的主数据流可以理解为一条从输入到评估的闭环。最前端是游戏画面或视频流，随后进入视觉检测模块完成目标识别，再进入目标状态表示和优先级筛选，接着进入预测与轨迹规划层，最后通过控制层输出相对鼠标动作。对控制行为、轨迹形态和时间节奏的记录随后又会回流到行为分析与反作弊检测模块，形成可用于训练和评估的研究闭环。[cite: 1]

## 2. 主模块与子模块功能说明

### 2.1 vision

vision 是视觉理解总层，负责把原始图像、视频或屏幕流转成可被研究链路消费的视觉对象。[cite: 1] 它位于整个系统的最前端，解决“如何稳定得到目标、轨迹和视觉上下文”的问题。[cite: 1]

vision.detection 的职责是目标检测，输出目标框、置信度和类别信息，作为后续目标状态转换的原材料。[cite: 1] 它解决的是视觉识别层面的问题，即从画面中找出人、头部、身体或其他研究对象，并把它们表述成统一的检测结果。[cite: 1]

vision.stream 的职责是图像与视频流接入，负责从本地视频、摄像头、桌面抓取或其他流源稳定提供逐帧图像。[cite: 1] 它位于视觉输入与推理之间，解决的是“如何低延迟、可控地获取帧”的问题，并为实时实验、离线视频回放和桌面采集提供统一入口。[cite: 1]

vision.tracking 的职责是多目标追踪和跨帧关联，用于维持目标身份、连接连续帧中的目标中心点，并研究遮挡、切换与轨迹稳定性。[cite: 1] 它位于检测之后、预测之前，解决的是“如何把每一帧的检测框组织成连续轨迹”的问题。[cite: 1]

vision.prediction 的职责是短期运动预测，辅助估计目标下一时刻可能出现的位置或偏移。[cite: 1] 它位于检测与控制之间，解决的是“目标会动，控制应提前多少”的问题，减少滞后与追踪误差。[cite: 1]

vision.benchmark 的职责是视觉侧基准测试入口，用于比较检测、追踪、预测与相关视觉任务在不同数据和场景下的表现。[cite: 1] 它位于研究验证层，解决的是“视觉模型在不同输入条件下是否稳定、是否可复现”的问题。[cite: 1]

vision.stream 内部当前已经形成了围绕桌面抓取、实时闭环和安全控制的完整支持链路，能够把抓到的 ROI 帧直接喂给后续的检测与控制模块。[cite: 1] 这一层是视觉输入能否真正接到控制输出的关键枢纽。[cite: 1]

### 2.2 cv_agent

cv_agent 是计算机视觉代理层，负责把视觉检测结果变成可执行的控制决策。[cite: 1] 它位于视觉理解和鼠标行为模拟之间，解决“检测到目标之后该选谁、该怎么走、该走多快、该如何输出动作”的问题。[cite: 1]

cv_agent.detection 的职责是把检测框转换成目标状态，并显式表达目标相对于屏幕中心的偏移。[cite: 1] 它处在视觉检测和目标筛选之间，是整个系统中把几何信息转成控制坐标的关键桥梁。[cite: 1]

cv_agent.selection 的职责是根据优先级规则在多个候选目标中选出当前要锁定的对象。[cite: 1] 它位于目标状态之后、预测和轨迹之前，解决的是“在多个目标里应该跟哪个”的问题。[cite: 1]

cv_agent.prediction 的职责是为目标移动提供前瞻估计，当前主要承载卡尔曼滤波一类的短期预测逻辑。[cite: 1] 它位于目标筛选后、轨迹规划前，解决的是“当前看到的位置和下一刻真正该对准的位置之间的差值”问题。[cite: 1]

cv_agent.trajectory 的职责是生成多风格的轨迹描述，用于表达从当前偏移到目标偏移的移动路径。[cite: 1] 它位于预测之后、控制之前，解决的是“鼠标不只是到达目标，还要以哪种方式到达”的问题。[cite: 1]

cv_agent.timing 的职责是管理轨迹执行的时间节奏和步进节拍。[cite: 1] 它位于轨迹生成和鼠标输出之间，解决的是“每一步什么时候发出、步间隔多长”的问题，从而影响平滑性、延迟和行为特征。[cite: 1]

cv_agent.control 的职责是把规划好的轨迹真正转换成相对鼠标输出或实验中的控制动作。[cite: 1] 它位于整个链路最末端，解决的是“如何把算法决策落到系统行为上”的问题，是视觉研究向动作模拟落地的出口。[cite: 1]

### 2.3 behavior

behavior 是行为研究层，负责把玩家、代理和 bot 的动作过程整理成可分析的数据资产。[cite: 1] 它位于控制输出之后、检测模型训练之前，解决的是“怎么从行为中提取可学习的模式”的问题。[cite: 1]

behavior.collection 负责行为数据采集与数据集切分，决定什么数据被收集、如何组织、如何分配到训练、验证和测试中。[cite: 1] 它是行为研究的数据入口。[cite: 1]

behavior.preprocessing 负责行为数据清洗、对齐和标准化，确保不同来源的数据可以进入统一分析流程。[cite: 1] 它位于采集和建模之间，解决的是“原始行为记录不整齐”的问题。[cite: 1]

behavior.features 负责特征构造，把鼠标轨迹、视角变化、反应时间和目标切换等信息转成模型可用的表征。[cite: 1] 它位于预处理之后、统计与建模之前，是行为检测可用性的关键步骤。[cite: 1]

behavior.statistics 负责统计分析、分布观察和行为指标计算，用于理解正常行为与异常行为的差异。[cite: 1] 它是分析层的重要入口，帮助研究者判断行为特征是否有区分度。[cite: 1]

behavior.visualization 负责把行为数据和统计结果可视化，便于人工审查、汇报和实验对比。[cite: 1] 它位于分析结果输出侧，解决的是“如何让结果更直观”的问题。[cite: 1]

### 2.4 anticheat

anticheat 是反作弊检测层，负责识别行为轨迹、时间序列和统计特征中的异常模式。[cite: 1] 它位于行为分析的上游消费端，解决的是“这些行为像不像人”以及“是否存在系统性辅助痕迹”的问题。[cite: 1]

anticheat.rules 负责规则型检测，适合表达明确、可解释的简单判定逻辑。[cite: 1] 它解决的是“先用可解释规则快速筛掉明显异常”的问题。[cite: 1]

anticheat.statistical 负责统计异常检测，关注分布偏差、频次偏差和时序异常。[cite: 1] 它处于规则和机器学习之间，解决的是“行为在统计上是否偏离正常模式”的问题。[cite: 1]

anticheat.classical_ml 负责传统机器学习检测，适合处理结构化特征和中小规模数据集。[cite: 1] 它解决的是“在特征已经整理好的前提下如何做分类或评分”的问题。[cite: 1]

anticheat.sequence_models 负责序列建模，适合处理连续行为、轨迹和时间依赖特征。[cite: 1] 它位于时序行为建模层，解决的是“单点特征不够时如何利用前后上下文”的问题。[cite: 1]

anticheat.transformer 负责基于 Transformer 的高阶序列建模与表征学习。[cite: 1] 它用于更复杂的时序关联和长程依赖检测，解决的是“复杂行为模式如何被更强的序列模型捕捉”的问题。[cite: 1]

anticheat.ensemble 负责多模型集成，把不同层次、不同类型的检测结果融合成更稳定的最终判断。[cite: 1] 它解决的是“单个模型不稳定时如何提升鲁棒性和覆盖度”的问题。[cite: 1]

### 2.5 adversarial

adversarial 是对抗研究层，主要研究如何规避检测、如何提升鲁棒性、以及如何在存在扰动时维持系统表现。[cite: 1] 它位于反作弊研究与攻击模拟之间，解决的是“检测与规避如何相互作用”的问题。[cite: 1]

adversarial.evasion 关注规避策略，研究行为如何更像自然人、如何降低可检测性。[cite: 1] 它是对抗研究中的攻击侧入口。[cite: 1]

adversarial.robustness 关注鲁棒性，研究在扰动、噪声和分布偏移下模型还能不能保持稳定。[cite: 1] 它位于防御侧，解决的是“检测模型在变化场景里是否还可靠”的问题。[cite: 1]

adversarial.adversarial_training 关注对抗训练思路，用于提升检测模型面对规避样本时的稳健程度。[cite: 1] 它连接攻击与防御两侧，是提升系统耐受性的训练策略层。[cite: 1]

### 2.6 benchmarks

benchmarks 是统一基准测试层，负责比较不同子系统、不同策略和不同模型配置的效果。[cite: 1] 它位于研究验证和实验输出之间，解决的是“各模块到底表现怎样、是否可比”的问题。[cite: 1]

benchmarks.vision 负责视觉任务基准，面向检测、追踪与预测相关实验。[cite: 1] 它衡量的是视觉链路的稳定性与准确度。[cite: 1]

benchmarks.behavior 负责行为分析基准，面向行为特征、分类和统计实验。[cite: 1] 它衡量的是行为表征与模型的有效性。[cite: 1]

benchmarks.anticheat 负责反作弊模型基准，面向规则、统计、机器学习和序列模型的比较。[cite: 1] 它衡量的是不同检测策略的识别能力和误报风险。[cite: 1]

benchmarks.adversarial 负责对抗研究基准，面向规避策略、鲁棒性和分布偏移测试。[cite: 1] 它衡量的是模型在压力条件下的抗扰动能力。[cite: 1]

### 2.7 visualization

visualization 负责实验结果叠加、分析图层和可视化输出。[cite: 1] 它位于研究过程的展示侧，解决的是“如何把检测、偏移、轨迹和统计结果直观呈现出来”的问题。[cite: 1]

它既服务于视觉调试，也服务于行为分析报告与实验汇总，是让系统结果可读、可审、可讲述的重要辅助层。[cite: 1]

## 3. 核心入口与脚本功能说明

项目根目录下的 train.py 是训练入口，承担模型训练与实验准备的工作，主要面向检测模型与相关学习过程。[cite: 1] 它处在数据集和模型产物之间，是从数据走向模型的入口。[cite: 1]

test_onnx.py 是导出模型的推理验证入口，主要用于检查 ONNX 模型是否可用、是否能在当前环境中完成推理。[cite: 1] 它位于模型导出后、部署前的验证阶段。[cite: 1]

test_video.py 是离线视频推理验证入口，用于对固定视频文件进行检测和结果查看。[cite: 1] 它主要用于验证视觉模型在视频流上的稳定性和可视化输出效果。[cite: 1]

scripts/offset_visualize.py 是偏移可视化与轨迹实验脚本，主要用于把检测结果、目标状态和轨迹策略叠加到图像上观察。[cite: 1] 它位于视觉检测和轨迹分析之间，适合调试偏移、查看目标选择是否合理，以及比较不同轨迹风格的差异。[cite: 1]

scripts/mouse_lock_smoke.py 是鼠标锁定与轨迹分类的烟雾测试脚本，主要用于验证轨迹生成、平滑度统计和相对鼠标输出是否能在最小路径上跑通。[cite: 1] 它位于控制层的快速验证入口，是从规划到输出的短链路测试工具。[cite: 1]

scripts/offline_benchmark.py 是离线逐级功能测试脚本，用于静态图像、离线视频和低速演示 bot 的分级基准验证。[cite: 1] 它位于系统级验证入口，适合在没有真实测试素材时先确认链路能跑通，再逐步替换为真实资源。[cite: 1]

## 4. 资源与数据目录说明

datasets 目录是项目的数据中心，存放不同来源、不同阶段、不同用途的数据集。[cite: 1] 它包括 human、bot、cv_agent、adversarial 以及与具体实验相关的子目录，承担原始输入、切片帧、轨迹和标签等内容的组织职责。[cite: 1]

datasets.human 用于保存真实玩家行为数据，通常包含原始录制、分帧结果、轨迹和标签。[cite: 1] 它是研究自然人行为模式的主要来源。[cite: 1]

datasets.bot 用于保存游戏 bot 或低速演示 bot 的行为数据。[cite: 1] 它用于与人类行为进行对照，帮助发现机器行为的规律性和边界。[cite: 1]

datasets.cv_agent 用于保存视觉代理或控制代理生成的行为数据。[cite: 1] 它是研究视觉辅助行为特征的重要中间层数据集。[cite: 1]

datasets.adversarial 用于保存对抗扰动、规避实验和鲁棒性测试相关数据。[cite: 1] 它为检测模型的压力测试和分布偏移研究提供素材。[cite: 1]

datasets.cs2_custom 是当前项目中与 CS2 定制数据相关的目录，承载训练、验证以及自定义标注数据。[cite: 1] 它是视觉模型实验中最直接的任务数据来源之一。[cite: 1]

models 目录用于存放模型产物和中间版本，便于实验迭代、回溯与比较。[cite: 1] 它处于训练输出侧，是模型管理的主要位置。[cite: 1]

weights 目录用于存放预训练权重、实验权重或外部导入的模型文件。[cite: 1] 它位于训练、推理和测试之间，属于可直接被脚本调用的模型资源层。[cite: 1]

runs 目录用于存放训练结果、推理结果和实验输出，包括检测、预测、分析和可视化产物。[cite: 1] 它是实验过程的结果沉淀区，也是调试时最常查看的输出目录之一。[cite: 1]

experiments 目录用于存放实验配置、运行日志和研究结果归档。[cite: 1] 它位于实验管理层，适合做跨实验对照、参数记录和复现实验。[cite: 1]

## 5. 典型链路位置说明

如果从数据流角度理解，vision 负责“看见”，cv_agent 负责“决定怎么动”，behavior 负责“记录动了什么”，anticheat 负责“判断像不像异常”，adversarial 负责“研究如何绕过或增强”，benchmarks 负责“比较谁更好”，visualization 负责“让结果可见”。[cite: 1]

这个顺序非常重要，因为它决定了每个模块的边界。[cite: 1] 视觉层不应该承担行为判别，控制层不应该负责训练，反作弊层不应该直接处理原始像素，而是应该消费已结构化的行为特征。[cite: 1]

## 6. 给其他 LLM 的协作与任务拆解建议

如果要把任务分配给其他 LLM，建议先按边界拆，再按数据流拆。[cite: 1] 也就是说，先告诉它需要处理的是视觉、控制、行为、反作弊还是对抗研究，再告诉它处在链路的哪一段，这样更容易得到准确答案。[cite: 1]

对于编码任务，建议优先把问题限定到单一模块，例如只让它改视觉输入层、只让它改目标筛选层、只让它改轨迹规划层，避免把检测、预测、控制和评估混在一起。[cite: 1] 这样可以减少耦合，方便验证，也更容易排查问题来源。[cite: 1]

对于审查任务，建议要求它先看输入输出，再看异常处理，最后看与上下游模块的接口是否一致。[cite: 1] 这样能快速发现数据类型、流程断点和未接通的模块。[cite: 1]

对于实验任务，建议先使用离线图像、离线视频和低速演示 bot 这三种可控资源做逐级验证，再过渡到更动态的环境。[cite: 1] 这样可以确保每一步链路都先在可控条件下通过，再进入更复杂的测试阶段。[cite: 1]

## 7. 项目理解顺序建议

如果是第一次接触这个仓库，建议先理解视觉输入与控制输出，再理解行为分析与反作弊，最后理解对抗和基准体系。[cite: 1] 这样能先抓住主链路，再扩展到研究与评估层。[cite: 1]

如果目标是做功能开发，优先看 vision 和 cv_agent。[cite: 1] 如果目标是做检测模型，优先看 behavior 和 anticheat。[cite: 1] 如果目标是做稳健性研究，优先看 adversarial 和 benchmarks。[cite: 1] 如果目标是做展示和汇报，优先看 visualization 和 runs 的结果输出。[cite: 1]

## 8. 当前仓库的整体性质

这个项目更像一个研究型框架，而不是单一功能程序。[cite: 1] 它把视觉理解、控制模拟、行为统计、异常检测和对抗研究组织到同一个仓库中，便于持续扩展和交叉验证。[cite: 1]

对协作人员和其他 LLM 来说，最重要的是记住模块边界和数据流方向。[cite: 1] 只要按“看见、筛选、预测、规划、输出、记录、识别、对抗、评估、可视化”这一顺序去理解，整个项目的结构就会非常清楚。[cite: 1]