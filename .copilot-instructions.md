# AI Game Security Lab - Copilot 代码生成约束

> **🔴 CRITICAL CONSTRAINT** - 防止 Codex 将 apply_mouse 改回受限版本

此文件被 GitHub Copilot、Codex 等 AI 代码工具读取，用于确保生成的代码遵循安全架构。

---

## 最核心的 3 条规则

### ✔ 规则 1：apply_mouse 必须动态可配置
- `apply_mouse` 不能被硬编码为 `False`
- 必须通过 `--apply-mouse` 命令行参数或 `RealtimeLoopConfig` 动态控制
- 默认值为 `False`（干运行）

### ✔ 规则 2：CSV 记录无条件启用
- 所有的控制操作都必须经过 `CSVLoggerBackend.send_relative_move()`
- 干运行模式调用 `controller.log_only()` → CSV 记录
- 活动模式调用 `controller.apply_correction()` → CSV + 真实控制
- 不允许存在跳过 CSV 的代码路径

### ✔ 规则 3：ChainedBackend + 多后端链式架构
- `cv_agent/control/backends/chained.py` 不能删除或重命名
- `create_mouse_backend(..., always_include_csv=True)` 必须返回链式后端
- Win32 后端必须支持通过 `custom_handler` 注入真实驱动
- `run_real.py` 的 Hook 机制必须保留

---

## 完整约束详见

📄 **`.github/copilot-instructions.md`**（本目录，约 500 行详细规范）

包含内容：
- 详细的代码示例（✅ 正确示例 vs ❌ 错误示例）
- 控制层、后端层、循环层的每个细节约束
- 数据落盘与日志保证机制
- 紧急安全机制（ESC/F12 热键等）
- 违规检查清单（6 类 20+ 项）

---

## 适用范围

此约束对以下所有操作强制生效：

- ✔ **代码生成**：Copilot 创建新代码
- ✔ **重构建议**：Copilot 提议重组现有代码
- ✔ **Bug 修复**：即使在修复 Bug 时也不能违反约束
- ✔ **性能优化**：优化内部实现但保持行为
- ✔ **功能扩展**：添加新功能必须符合约束

---

## 快速检查

在提交 PR 前，确保：

```bash
# 1. apply_mouse 未被硬编码
grep -r "apply_mouse.*=.*False" cv_agent/ vision/ \
  | grep -v "RealtimeLoopConfig" && echo "❌ FAIL" || echo "✅ PASS"

# 2. ChainedBackend 存在
[ -f "cv_agent/control/backends/chained.py" ] && echo "✅ PASS" || echo "❌ FAIL"

# 3. CSV 后端可用
grep -r "CSVLoggerBackend" cv_agent/control/factory.py > /dev/null && echo "✅ PASS" || echo "❌ FAIL"
```

---

## 违规后果

- 🚫 PR 将被自动拒绝（CI 检查失败）
- 📋 详细的违规报告会发布到 PR 评论
- 👤 需要维护者手动审查并修正
- 🔒 如果多次违规，将启用更严格的代码审查策略

---

## 联系与讨论

- **报告问题**：GitHub Issues（标记 `[COPILOT-COMPLIANCE]`）
- **提议更新**：GitHub Discussions
- **紧急情况**：直接联系项目维护者
